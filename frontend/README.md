# Frontend

React + Vite. Как поднять весь проект — в [корневом README](../README.md).

Локальный `.env` (копия с [`.env.example`](.env.example)) в git не коммитится. На Vercel Root Directory — `frontend`; прод ходит на `/graphql` через [`vercel.json`](vercel.json). Деплой: [docs/DEPLOY.md](../docs/DEPLOY.md).
